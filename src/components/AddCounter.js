const AddCounter = ({ addCounter }) => {
  return (
    <>
      <button className="buttonAddCounter" onClick={addCounter}>
        Add Counter
      </button>
    </>
  );
};

export default AddCounter;
